# 测试验证目录说明

本目录作为企业 SDK 的统一验证入口保留。

当前已验证的测试资产位于：

- `../GhostHub_SDK/tests/`
- `../GhostHub_SDK/test_sdk.py`

2026-04-16 的最新校验结果：

- `pytest tests -q` -> `76 passed`

如果后续需要追加验收脚本、基准测试或 CI 导出结果，可集中放回本目录。
