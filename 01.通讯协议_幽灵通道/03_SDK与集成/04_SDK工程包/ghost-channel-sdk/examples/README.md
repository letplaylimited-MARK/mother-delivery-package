# JSON Examples

本目录包含与 `schemas/` 一一对应的示例 JSON 文件，用于：

1. 人工阅读
2. schema 校验
3. SDK 集成示例

## 映射清单

本目录包含：

- `example-schema-map.json`

它定义了 example → schema 的显式映射关系，并已纳入本地 validator / pre-commit / CI 检查。

建议后续将这些示例纳入自动化校验流程。
