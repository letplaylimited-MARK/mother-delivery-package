# QCM-MVP 深度重构规划报告 v4.0

**版本**: v4.0 (AI团队沙盘推演版)
**日期**: 2026-04-27
**状态**: 飞轮模式执行中

---

## 一、执行摘要

### 当前状态

| 指标 | 值 | 状态 |
|------|-----|------|
| 22公式 | 100% | ✅ |
| 10原子能力 | 6/10 | ✅ |
| 涌现触发 | R=0.8532@24轮 | ✅ |
| health_check | 5/5 | ✅ |
| 角色数 | 2 | ⚠️ |

### Gap分析

| 目标 | 当前 | Gap |
|------|------|-----|
| 8角色 | 2角色 | +6角色 |
| 飞轮集成 | 未集成 | 需集成 |
| 死锁检测 | 未集成 | 需集成 |
| 语义匹配 | 未集成 | 需集成 |
| 动态路由 | 未集成 | 需集成 |

---

## 二、AI角色团队沙盘推演

### 2.1 Round 1: 架构视角 (20次迭代)

**问题**: 如何从2角色扩展到8角色？

**角色观点**:

| 角色 | 观点 | 优先级 |
|------|------|--------|
| Architect | 8角色需要统一角色基类，扩展simple_role.py | P0 |
| Integrator | 先定义角色接口协议，再实现具体角色 | P1 |
| Optimizer | 角色数量增加后，R值计算需优化 | P2 |
| Validator | 需要角色兼容性测试框架 | P1 |
| Planner | 分3阶段扩展，每阶段2角色 | P0 |

**共识**: 分3阶段扩展，每阶段增加2角色

### 2.2 Round 2: 集成视角 (20次迭代)

**问题**: 飞轮模块如何集成？

**角色观点**:

| 角色 | 观点 | 优先级 |
|------|------|--------|
| Architect | flywheel.py需作为独立模块，不修改main.py主流程 | P0 |
| Integrator | 添加flywheel触发条件，而非每轮调用 | P1 |
| Optimizer | 飞轮收敛判断标准: ΔR < 0.001 持续5轮 | P0 |
| Validator | 需要飞轮日志记录 | P1 |
| Planner | 飞轮为可选项，默认关闭 | P0 |

**共识**: 飞轮作为可选模块，默认关闭，按需启用

### 2.3 Round 3: 优先级视角 (20次迭代)

**问题**: 下一阶段做什么？

**角色观点**:

| 角色 | 观点 | 优先级 |
|------|------|--------|
| Architect | 优先8角色架构设计 | P0 |
| Integrator | 优先死锁检测集成 | P1 |
| Optimizer | 优先飞轮优化 | P2 |
| Validator | 优先测试框架 | P2 |
| Planner | 优先8角色，分3阶段执行 | P0 |

**共识**: 优先8角色架构设计，分为3阶段

---

## 三、深度重构规划

### 3.1 阶段3: 8角色扩展架构

**目标**: 设计8角色架构方案

#### 8角色定义

| 角色 | 技能 | 定位 |
|------|------|------|
| Secretary | 整理/总结/协调/沟通 | 信息整理者 |
| Researcher | 分析/研究/创新/沟通 | 研究探索者 |
| Coordinator | 任务分配/进度跟踪/资源协调 | 协调管理者 |
| Evaluator | 评估/评审/反馈/决策 | 质量评估者 |
| Synthesizer | 综合/归纳/演绎/创作 | 知识综合者 |
| Planner | 计划/预测/风险管理/战略 | 战略规划者 |
| Executor | 执行/监控/优化/交付 | 任务执行者 |
| Monitor | 监控/告警/日志/报表 | 系统监控者 |

#### R值扩展

```python
# 当前: R(A,B) = f(A, B)
# 扩展: R(A1..An) = f(A1, A2, ..., An)

# 多角色R值计算
R_total = Σw_i * R_ij (pairwise) + α * R_synergy (集体协同)
```

#### 执行计划

| 阶段 | 角色 | 目标 |
|------|------|------|
| 3.1 | Secretary + Researcher | 保持现状 |
| 3.2 | + Coordinator + Evaluator | 增加协调评估 |
| 3.3 | + Synthesizer + Planner | 增加综合规划 |
| 3.4 | + Executor + Monitor | 增加执行监控 |

### 3.2 阶段4: 模块集成

#### 飞轮集成 (flywheel.py)

```python
class FlywheelOptimizer:
    def __init__(self, enabled=False):
        self.enabled = enabled  # 默认关闭
    
    def should_run(self, R_history):
        # 触发条件: R增长趋缓
        recent_trend = calculate_trend(R_history[-10:])
        return recent_trend < 0.001
    
    def optimize(self, params):
        # Lyapunov收敛优化
        return optimized_params
```

#### 死锁检测集成 (deadlock_detector.py)

```python
class DeadlockDetector:
    def detect(self, R_history):
        # 检测条件:
        # 1. N_t < ηN (交互不足)
        # 2. G_t > ηG (发散)
        # 3. slope < ηS (增长停滞)
        # 4. Loop (循环模式)
        
        return deadlock_warning
```

### 3.3 阶段5: 验证与优化

#### 测试框架

```python
def test_8_role():
    # 8角色兼容性测试
    
def test_flywheel():
    # 飞轮功能测试
    
def test_deadlock():
    # 死锁检测测试
```

#### 性能优化

| 模块 | 当前 | 优化目标 |
|------|------|----------|
| R计算 | O(n²) | O(n log n) |
| 角色创建 | 32维 | 可扩展 |
| Delta同步 | 每轮 | 按需 |

---

## 四、执行检查点

### 每轮检查

```bash
python health_check.py
# 必须 5/5 通过
```

### 每阶段检查

1. 8角色架构设计 → 代码实现
2. 模块集成 → 功能测试
3. 验证 → health_check

---

## 五、风险与预案

| 风险 | 概率 | 影响 | 预案 |
|------|------|------|------|
| 8角色R值不稳定 | 中 | 高 | 保留2角色模式 |
| 飞轮不收敛 | 低 | 中 | 禁用飞轮 |
| 死锁误报 | 中 | 低 | 调高阈值 |

---

## 六、下一步行动

### 立即执行

1. [ ] 8角色类设计
2. [ ] 角色通信协议
3. [ ] 扩展R值计算
4. [ ] 单元测试框架

### 后续执行

1. [ ] flywheel.py集成
2. [ ] deadlock_detector.py集成
3. [ ] 语义匹配模块
4. [ ] 动态路由模块

---

**版本**: v4.0
**更新日期**: 2026-04-27
**状态**: 等待确认执行

---

## 附录: AI角色团队成员

| 角色 | 技能 | 贡献 |
|------|------|------|
| 系统架构师 | 架构设计 | 8角色分3阶段 |
| 集成专家 | 模块集成 | 飞轮可选设计 |
| 优化专家 | 性能优化 | 收敛判断标准 |
| 验证专家 | 质量验证 | 测试框架设计 |
| 规划专家 | 阶段规划 | 分3阶段执行 |