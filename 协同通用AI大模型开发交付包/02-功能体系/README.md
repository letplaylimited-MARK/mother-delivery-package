# 功能体系

> 用途：说明项目具体能做什么、用户如何使用、AI 能力边界在哪里。

## 1. 功能总览

| 功能 | 用户价值 | 输入 | 输出 | 状态 |
|---|---|---|---|---|
| `<功能名称>` | `<解决什么问题>` | `<输入>` | `<输出>` | `<已完成/待开发/可选>` |

## 2. 用户流程

```text
用户进入项目
  -> 完成初始化/登录/配置
  -> 输入任务或数据
  -> 系统/AI 处理
  -> 用户查看结果
  -> 用户确认、导出、继续迭代
```

按真实项目补充关键流程：

| 流程 | 步骤 | 预期结果 |
|---|---|---|
| `<流程名称>` | `<步骤 1 -> 步骤 2 -> 步骤 3>` | `<用户得到什么>` |

## 3. AI 能力

| AI 能力 | 触发方式 | 使用数据 | 输出 | 人工确认点 |
|---|---|---|---|---|
| `<能力名称>` | `<按钮/API/自动任务/提示词>` | `<数据来源>` | `<结果>` | `<是否需要人工复核>` |

## 4. 配置与权限

| 项 | 说明 |
|---|---|
| 账号/权限 | `<填写角色、权限、访问限制>` |
| 配置项 | `<填写配置文件、环境变量、模型参数>` |
| 外部依赖 | `<填写 API、数据库、模型、第三方服务>` |

## 5. 异常与失败处理

| 异常 | 用户可见表现 | 处理方式 |
|---|---|---|
| `<异常类型>` | `<报错/空结果/超时>` | `<恢复方式>` |
