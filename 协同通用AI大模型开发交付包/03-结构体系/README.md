# 结构体系

> 用途：说明项目由哪些文件、模块、数据、接口、依赖组成，方便用户、开发者和 AI 快速理解。

## 1. 目录地图

```text
<用户交付包根目录>/
├── README.md
├── 01-价值体系/
├── 02-功能体系/
├── 03-结构体系/
├── 04-运作体系/
├── app/ 或 src/
├── scripts/
├── tests/
└── docs/
```

按真实项目更新目录含义：

| 路径 | 作用 | 是否必须 |
|---|---|---|
| `<路径>` | `<作用>` | `<必须/可选>` |

## 2. 模块职责

| 模块 | 职责 | 上游 | 下游 |
|---|---|---|---|
| `<模块名称>` | `<负责什么>` | `<依赖谁>` | `<被谁调用>` |

## 3. 数据流

```text
用户输入/外部数据
  -> 数据校验
  -> 核心处理/AI 推理
  -> 结果生成
  -> 存储/导出/展示
```

## 4. 接口与集成

| 接口 | 类型 | 调用方 | 返回/效果 |
|---|---|---|---|
| `<接口名称>` | `<CLI/API/MCP/Webhook/文件>` | `<调用方>` | `<返回内容>` |

## 5. 依赖与配置

| 类型 | 内容 | 说明 |
|---|---|---|
| 运行时 | `<Python/Node/系统依赖>` | `<版本要求>` |
| 数据库 | `<SQLite/Postgres/文件>` | `<位置与初始化方式>` |
| 模型/API | `<模型或服务>` | `<配置方式，不写真实密钥>` |
| 配置文件 | `<配置路径>` | `<字段说明>` |

## 6. 构建与产物

| 产物 | 生成方式 | 用途 |
|---|---|---|
| `<产物路径>` | `<命令>` | `<用途>` |
